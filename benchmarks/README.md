# Lyra Benchmarks

This directory will hold reproducible compiler, runtime, package-manager, AI, and embedded benchmarks.

Performance claims should be backed by repeatable measurements rather than aspirational targets.
